name = "detection-rule-forge"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Set secrets with:
#   wrangler secret put ANTHROPIC_API_KEY
#
# Optional vars (safe to keep in this file or override with `wrangler secret put`):
[vars]
MODEL_ID = "claude-sonnet-5"
ALLOWED_ORIGIN = "*"
