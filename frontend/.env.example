# URL of your deployed Cloudflare Worker (see /worker).
# Locally this defaults to http://127.0.0.1:8787/generate if unset.
VITE_API_ENDPOINT=https://detection-rule-forge.YOUR-SUBDOMAIN.workers.dev/generate
